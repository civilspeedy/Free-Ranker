{
    "name": "free-ranker",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview",
        "lint": "oxlint",
        "lint:fix": "oxlint --fix"
    },
    "dependencies": {
        "@preact/signals": "^2.9.0",
        "preact": "^10.29.0"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.10.5",
        "@types/node": "^24.12.0",
        "oxlint": "^1.57.0",
        "typescript": "^6.0.2",
        "vite": "^8.0.3"
    }
}
